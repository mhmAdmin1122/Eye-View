import { Inter, Fjalla_One } from 'next/font/google'
// const inter = Inter({ subsets: ['latin'] })
// const fjalla_One = Fjalla_One({ subsets: ['latin'], weight: "400", display: 'swap'});
export const inter = Inter({
    subsets: ['latin'],
    display: 'swap',
});

export const fjalla_One = Fjalla_One({
    weight: "400",
    subsets: ['latin'],
    display: 'swap',
});